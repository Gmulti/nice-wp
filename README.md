# Theme Twentyseventeen React

> This theme corresponds to the default twentyseventeen theme with ReactJS modules. It serves as an example for the WordCamp Nice workshop

## Install

-   `git clone`
-   `npm install`

## Compile

-   Build : `webpack`
-   Watch : `webpack -w`
