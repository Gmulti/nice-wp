export const posts = (
    state = {
        list: [],
        single: null
    },
    { type, payload }
) => {
    switch (type) {
        default:
            return state
    }
}

export default posts
